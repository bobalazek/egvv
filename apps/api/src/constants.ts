import { join } from 'path';

export const ASSETS_DIRECTORY = join(__dirname, '..', '..', '..', 'libs', 'shared', 'assets', 'src', 'assets');
export const ASSETS_SERIES_DIRECTORY = join(ASSETS_DIRECTORY, 'series');
